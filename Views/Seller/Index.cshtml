@model IEnumerable<House_Rental_System.Models.Property_Details>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_SellerLayout.cshtml";
}

<p>@ViewBag.property</p>
<table class="table">
    <tr>
        <th>
            Type
            
        </th>
        <th>
            Name
            
        </th>
        <th>
            Address
            
        </th>
        <th>
            State
            
        </th>
        <th>
            City
            
        </th>
        <th>
            Pincode
            
        </th>
        <th>
            Status
            
        </th>

        <th></th>
    </tr>
    @{
        int i = 0;
        int[] property_count = ViewBag.pc;
    }
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Property_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Property_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Property_Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Property_State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Property_City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Property_Pin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Property_Status)
            </td>
           
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Property_ID }) |
                @{
                    if (property_count[i] == 1)
                    {
                    <button class="btn"><i class="fas fa-trash-alt" style="color:cornflowerblue"></i>@Html.ActionLink("Request", "Requests", new { id = item.Property_ID })</button>
                    }
                    else
                    {

                        <button class="btn" disabled><i class="fas fa-trash-alt" style="color:cornflowerblue"></i>Request</button>
                    }
                    i += 1;
                } |
                @Html.ActionLink("Delete", "DeleteProperty", new { id = item.Property_ID })
            </td>
        </tr>
     }

</table>

