@model IEnumerable<House_Rental_System.Models.Sold_Property>

@{
    ViewBag.Title = "SoldProperty";
    Layout = "~/Views/Shared/_SellerLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Deposit_Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rental_Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date_of_Sale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer_Details.Customer_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Property_Details.Property_Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Deposit_Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rental_Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date_of_Sale)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer_Details.Customer_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Property_Details.Property_Type)
        </td>   
    </tr>
}

</table>
